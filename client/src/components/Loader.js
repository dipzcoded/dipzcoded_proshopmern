import Circle from "better-react-spinkit/dist/Circle";

const Loader = () => {
  return (
    <center
      style={{
        marginTop: "100px",
      }}
    >
      <Circle size={60} color="#343A40" />
    </center>
  );
};

export default Loader;
